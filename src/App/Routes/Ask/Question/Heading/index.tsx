import * as React from 'react'
import styled from 'styled-components'

const Layout = styled.div`
  padding: 0 1rem;

  font-weight: bolder;
`

export const Heading: React.SFC = () => <Layout>다뉴에게 질문을 해봐요!</Layout>
